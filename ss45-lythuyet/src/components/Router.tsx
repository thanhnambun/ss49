import React from "react";

export default function Router() {
  return (
    <div>
      Router
      {/* 
            React router DOM: tạo bộ định tuyến cho trang web của mình,
            cho phép React element của mình được hiển thị ứng với đường dẫn URL
            - cách dùng: 
            + cài đặt: npm i react-router-dom
            dùng component brownseRouter để cung cấp bộ định tuyết cho toàn app.
        */}
        
    </div>
  );
}
